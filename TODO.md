# TODO

- [ ] Disable glass/backdrop blur effect inside the Create Request modal so the form remains fully visible and fillable.
  - [ ] Update `Frontend/css/styles.css` with a `.modal .card-glass` override (remove `backdrop-filter` / ensure proper background + z-index).
  - [ ] Verify by running the frontend and filling the Create Request modal form.

