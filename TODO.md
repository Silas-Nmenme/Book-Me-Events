# TODO

- [x] Fix SyntaxError in src/controllers/authController.js: duplicated `loginPassword` declarations.
- [x] Remove the stray duplicate variables and clean the login method so it compiles.
- [ ] Restart app (or run node) to confirm no further startup crashes.


