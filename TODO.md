# TODO
- [ ] Update `app.js` to stop silently swallowing announcement route load errors (log stack / rethrow for announcements).
- [ ] Re-deploy / re-run and verify `GET /api/v1/announcements?page=1&limit=10` works on Vercel.
- [ ] Mark TODO done once the endpoint returns 200.
- [x] Update `app.js` announcement route loader error handling.


- [ ] If auth is required, ensure frontend provides Authorization header when calling announcements.

